# Wallet used to deploy the contract (needs testnet BOT from the faucet)
DEPLOYER_PRIVATE_KEY=""

# Wallet the off-chain AI scoring service will use to call submitScore().
# Can be the same as the deployer for the hackathon demo; keep them separate
# in production so a compromised scoring-service key can't upgrade the contract.
SCORER_ADDRESS=""
SCORER_PRIVATE_KEY=
BOT_TESTNET_RPC=https://rpc.bohr.life
BOT_MAINNET_RPC=https://rpc.botchain.ai
BIZTRACE_CONTRACT_ADDRESS=